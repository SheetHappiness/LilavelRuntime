[project]
name = "lilavel-launcher"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lilavel-runtime"]

[tool.uv]
package = false

[tool.uv.sources]
lilavel-runtime = { path = "apps/runtime", editable = true }
